{
  "name": "lintp",
  "version": "0.1.3",
  "description": "File system linter with a custom DSL for directory structure validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "lintp": "./bin/lintp"
  },
  "scripts": {
    "build": "npm run build:ts && npm run build:rust",
    "build:ts": "tsc -p tsconfig.build.json",
    "build:rust": "cargo build --release",
    "check": "npm run check:ts && npm run check:rust",
    "check:ts": "tsc --noEmit",
    "check:rust": "cargo check",
    "test": "npm run test:ts && npm run test:rust",
    "test:ts": "echo \"Error: no test specified\" && exit 1",
    "test:rust": "cargo test",
    "sync-toml": "tsx scripts/sync-toml-to-package.ts",
    "prepublishOnly": "npm run build:ts && npm run sync-toml",
    "format": "npm run format:ts && npm run format:rust",
    "format:ts": "prettier --write .",
    "format:rust": "cargo fmt",
    "format:check": "npm run format:ts:check && npm run format:rust:check",
    "format:ts:check": "prettier --check .",
    "format:rust:check": "cargo fmt -- --check",
    "lint": "npm run lint:ts && npm run lint:rust",
    "lint:ts": "eslint .",
    "lint:rust": "cargo clippy -- -D warnings",
    "lint:fix": "npm run lint:ts:fix && npm run lint:rust:fix",
    "lint:ts:fix": "eslint . --fix",
    "lint:rust:fix": "cargo clippy --fix --allow-dirty --allow-staged",
    "prepare": "husky",
    "commit": "cz"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/narehart/lintp.git"
  },
  "keywords": [
    "linter",
    "file-system",
    "naming-conventions",
    "dsl",
    "validation",
    "cli",
    "rust",
    "fs",
    "path",
    "directory-structure"
  ],
  "author": "Nico Arehart",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/narehart/lintp/issues"
  },
  "homepage": "https://narehart.github.io/lintp/",
  "files": [
    "bin/",
    "dist/",
    "README.md"
  ],
  "engines": {
    "node": ">=14"
  },
  "os": [
    "darwin",
    "linux",
    "win32"
  ],
  "cpu": [
    "x64",
    "arm64"
  ],
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@commitlint/types": "^19.8.1",
    "@eslint/js": "^9.30.0",
    "@types/node": "^24.0.7",
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "eslint": "^9.30.0",
    "husky": "^9.1.7",
    "prettier-plugin-rust": "^0.1.9",
    "smol-toml": "^1.4.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0"
  }
}
