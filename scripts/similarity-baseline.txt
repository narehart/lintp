# Pairs similarity-rs reports that have been reviewed and accepted.
#
# Checked by scripts/check-similarity.sh. Anything reported and NOT listed here
# fails the build. This is a list to shrink: an entry is only justified when two
# functions share a shape rather than an implementation. Real duplication gets
# factored out instead of parked here.
#
# Each entry: <file>::<function> <-> <file>::<function>

# 91.54% — two dispatch-by-name functions. The score reflects the skeleton
# every interpreter has (`fn(name, ..) -> Result<Value> { match name { .. } }`)
# and does not move when the arms are rewritten; they dispatch different things
# and take different arguments.
src/dsl/functions.rs::call_lambda_function_impl <-> src/dsl/functions.rs::call_function_impl

# 87.26% and 85.67% — caller and callee. glob_function calls glob_in, and
# string_predicate calls binary_function; they already share the very
# implementation the tool is pointing at. Collapsing them would mean inlining
# the helper back into its callers.
src/dsl/functions.rs::glob_in <-> src/dsl/functions.rs::glob_function
src/dsl/functions.rs::binary_function <-> src/dsl/functions.rs::string_predicate

# 85.53% — the two public entry points, each a single line delegating to its
# _impl and mapping the error through the shared into_dsl_error.
src/dsl/functions.rs::call_lambda_function <-> src/dsl/functions.rs::call_function

# 86.41% — both validate arguments then hand off, but one globs a directory and
# the other parses a lambda out of a string.
src/dsl/functions.rs::glob_function <-> src/dsl/functions.rs::string_lambda_function

# 85.17% — both walk a rule key and return a Result, but one validates the key
# shape and the other expands brace alternation into several keys.
src/config.rs::validate_rule_key <-> src/config.rs::expand_rule_keys
